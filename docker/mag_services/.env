# MongoDB configuration
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=securepassword123
MONGO_DATABASE=mcp-agent-graph
MONGO_PORT=27017

# MongoDB Express configuration
MONGO_EXPRESS_PORT=8081
MONGO_EXPRESS_USERNAME=admin
MONGO_EXPRESS_PASSWORD=expresspwd123

# MinIO configuration
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123
MINIO_API_PORT=9010
MINIO_CONSOLE_PORT=9011
MINIO_SECURE=false

# ================================
# JWT Configuration
# ================================
# JWT_SECRET_KEY: 用于签名和验证JWT令牌的密钥
#
# ⚠️  安全要求：
#   - 必须是随机生成的复杂字符串
#   - 建议长度至少32字符
#   - 生产环境切勿使用简单密码（如"secret"、"password"等）
#   - 此密钥泄露将导致任何人都能伪造用户身份
#
# 🔧 生成方法：
#   运行命令: python mag/scripts/generate_jwt_secret.py
#   或使用: python -c "import secrets; print(secrets.token_urlsafe(32))"
#
# 📝 注意事项：
#   - 生成后请妥善保管，不要提交到版本控制系统
#   - 更换密钥会使所有已签发的Token立即失效
#   - 建议定期轮换（如每6-12个月）
#
JWT_SECRET_KEY=jHqVM8XhApyMhTb3EF3T3IQJorteDtOCHLj7V1G67VFkbp8GOInOBCaOIs84a1agV3f_1kB59yovh7Ez5PMEaQ

# JWT_ALGORITHM: JWT签名算法（推荐使用HS256）
JWT_ALGORITHM=HS256

# JWT_ACCESS_TOKEN_EXPIRE_MINUTES: Access Token过期时间（分钟）
# 默认15分钟，用于日常API访问
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=15

# JWT_REFRESH_TOKEN_EXPIRE_DAYS: Refresh Token过期时间（天）
# 默认7天，用于刷新Access Token
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# Super Admin configuration
ADMIN_USERNAME=yyx
ADMIN_PASSWORD=yyx123